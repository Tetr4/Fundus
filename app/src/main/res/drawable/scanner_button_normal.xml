<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- outer border -->
    <item android:drawable="@drawable/scanner_border"/>

    <!-- inner frame -->
    <item
        android:bottom="@dimen/scanner_border"
        android:left="@dimen/scanner_border"
        android:right="@dimen/scanner_border">
        <shape
            xmlns:android="http://schemas.android.com/apk/res/android"
            android:shape="rectangle" >
            <solid android:color="@color/scanner_frame" />

            <corners
                android:bottomLeftRadius="@dimen/scanner_border_radius"
                android:bottomRightRadius="@dimen/scanner_border_radius" />
        </shape>
    </item>

    <!-- barcode icon -->
    <item
        android:bottom="@dimen/scanner_icon_margin"
        android:left="@dimen/scanner_icon_margin"
        android:right="@dimen/scanner_icon_margin"
        android:top="@dimen/scanner_icon_margin">
        <bitmap android:src="@drawable/ic_barcode" />
    </item>

</layer-list>